# ============================================
# CLOUDFLARE WORKER ENVIRONMENT VARIABLES
# ============================================
# 
# NOTE: Cloudflare Workers use encrypted secrets, not .env files
# Set these using: wrangler secret put <NAME>
# 
# Example commands:
#   wrangler secret put BACKEND_URL
#   wrangler secret put WEBHOOK_SECRET
#   wrangler secret put WEBHOOK_PATH
#
# ============================================

# Backend API URL (your deployed backend or ngrok URL for local testing)
# Example: https://your-backend.com or https://abc123.ngrok.io
BACKEND_URL=https://your-backend-url.com

# Webhook secret (MUST match CF_WEBHOOK_SECRET in backend/.env)
WEBHOOK_SECRET=super-secret

# Webhook path on your backend (default: /api/webhooks/cloudflare)
WEBHOOK_PATH=/api/webhooks/cloudflare

# ============================================
# SETUP INSTRUCTIONS
# ============================================
# 
# 1. Install wrangler: npm install -g wrangler
# 2. Login: wrangler login
# 3. Set secrets:
#    - wrangler secret put BACKEND_URL
#    - wrangler secret put WEBHOOK_SECRET
#    - wrangler secret put WEBHOOK_PATH (optional)
# 4. Deploy: wrangler deploy
#
# For local testing with ngrok:
#   1. Start backend: cd backend && npm run dev
#   2. Start ngrok: ngrok http 4000
#   3. Update secret: wrangler secret put BACKEND_URL
#      Enter: https://xxxx.ngrok.io
#   4. Redeploy: wrangler deploy
#
# ============================================

